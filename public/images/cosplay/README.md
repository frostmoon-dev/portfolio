Drop cosplay photos here (e.g. `convention-2026-01.jpg`) and reference them
from the `PHOTOS` array in `components/hobbies/CosplayGallery.tsx` as
`src: "/images/cosplay/convention-2026-01.jpg"` to replace a placeholder
tile with a real photo.
