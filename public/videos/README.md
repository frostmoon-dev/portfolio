Drop video clips here (e.g. `design.mp4`) and reference them from the `TILES`
array in `components/Collage.tsx` as `video: "/videos/design.mp4"` to replace
a placeholder tile with a real looping video.
